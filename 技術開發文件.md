# dlyt／巔峰思維 — 技術開發與維護文件

> **版本：** v2.0  
> **最後更新：** 2026-08-05  
> **產品定位：** YouTube／Facebook／Instagram 影片與字幕下載（免費可公開部署）  
> **品牌：** 巔峰思維（[getzenithmind.com](https://www.getzenithmind.com/zh-TW)）  
> **本文件用途：** 記錄本次開發成果、系統架構、部署、維運與故障排除，供未來維護者完整接手。

---

## 目錄

1. [專案總覽](#1-專案總覽)
2. [目錄結構](#2-目錄結構)
3. [本機開發與啟動](#3-本機開發與啟動)
4. [後端架構（FastAPI）](#4-後端架構fastapi)
5. [前端架構（Next.js）](#5-前端架構nextjs)
6. [核心業務流程](#6-核心業務流程)
7. [API 契約完整清單](#7-api-契約完整清單)
8. [環境變數一覽](#8-環境變數一覽)
9. [部署（GitHub／Render／Vercel／Upstash）](#9-部署githubrendervercelupstash)
10. [安全、合規與濫用防護](#10-安全合規與濫用防護)
11. [測試與品質門檻](#11-測試與品質門檻)
12. [已知限制與技術債決策](#12-已知限制與技術債決策)
13. [故障排除手冊](#13-故障排除手冊)
14. [維護 checklist 與變更紀錄](#14-維護-checklist-與變更紀錄)

---

## 1. 專案總覽

### 1.1 做什麼

使用者貼上 **YouTube／Facebook／Instagram** 公開影片網址後可以：

| 模式 | 說明 |
|------|------|
| 影片下載 | 解析畫質清單 → 選擇格式 → 下載（優先直連，失敗則伺服器代理） |
| 字幕下載 | 下載既有字幕（SRT／VTT／TXT） |
| 語音辨識字幕（ASR） | 影片**沒有**可下載字幕時，使用者自備 Gemini API Key，由後端下載音訊後呼叫 Gemini 產生字幕 |

### 1.2 技術棧

| 層級 | 技術 | 免費託管建議 |
|------|------|----------------|
| 前端 | Next.js 16（App Router）+ React | Vercel Free |
| 後端 | Python 3.12 + FastAPI + yt-dlp | Render Free（Docker） |
| 狀態／快取 | 記憶體 **或** Redis（Upstash Free） | 正式環境強烈建議 Redis |
| 保活 | GitHub Actions cron ping `/health` | 減少 Render 休眠 |
| 影音處理 | 系統 ffmpeg（Docker 已裝）；本機可用 `imageio-ffmpeg` | — |

**不做 Celery／獨立 queue：** 下載在 FastAPI 同一進程以 `BackgroundTasks` + ThreadPool 執行，較符合免費方案資源限制。

### 1.3 產品／品牌注意

- UI 品牌為「**巔峰思維**」，不以「dlyt」作為頁面主視覺名稱（內部 repo／theme key 仍可能含 `dlyt`）。
- 首頁為 SSR 品牌區 + 輕量解析表單；重型下載 UI 採 **lazy load**（`DownloaderLazy`），以利 PageSpeed。
- Cookie／本機儲存需同意；僅「全部允許」才啟用訪客統計（presence）。

### 1.4 本輪（v2.0）開發成果摘要

| 類別 | 內容 |
|------|------|
| 下載品質 | 移除靜默 `/b` 格式 fallback；失敗要明確報錯；格式選擇器對齊具體 itag |
| ffmpeg | 本機可解析 `imageio-ffmpeg` 路徑給 yt-dlp；Docker 用系統 ffmpeg |
| Player clients | YouTube 多組 client 嘗試與字幕合併；避免只拿到 360p 卻誤以為成功 |
| 平台 | FB／IG URL 驗證與 yt-dlp 下載路徑 |
| ASR | Gemini 使用者自備 Key；時長／單檔預設不限（`0`） |
| 流量 | 月代理額度（本機預設約 90GB；Render Blueprint 悲觀約 40GB）；用盡大字彈窗 |
| 統計 | `/api/presence`、`/api/stats`；前端同源 BFF；Cookie 同意閘 |
| 合規 | `/privacy`、CookieConsent、JsonLd、SiteFooter、CSP、skip-link |
| 清理 | 刪除 Next.js mock download API、未用 SVG、樣板 README／AGENTS |
| 品質 | 後端 pytest 擴充；前端 lint／build 通過 |

---

## 2. 目錄結構

```
dlyt/
├── README.md                      # 給使用者／部署的精簡說明
├── 技術開發文件.md                # 本文件（維運完整版）
├── 測試報告.md                    # 推送前驗證紀錄
├── start.bat                      # 一鍵啟動前後端（Windows）
├── start-backend.bat              # 僅後端
├── render.yaml                    # Render Blueprint
├── .gitignore
├── .github/workflows/
│   ├── keepalive.yml              # 每 12 分鐘 ping 後端 /health
│   └── bump-yt-dlp.yml            # 每週自動升級 yt-dlp 並開 PR
├── backend/
│   ├── Dockerfile                 # 多階段：ffmpeg + Node 22 + uvicorn
│   ├── requirements.txt
│   ├── .env.example               # 後端環境變數範本（可進版控）
│   ├── .env                       # 本機密鑰／設定（禁止 commit）
│   ├── app/
│   │   ├── main.py                # FastAPI 入口、lifespan、middleware
│   │   ├── config.py              # Settings
│   │   ├── schemas.py             # Pydantic 模型
│   │   ├── middleware.py          # 安全標頭、維護模式
│   │   ├── logging_config.py
│   │   ├── routers/               # HTTP 路由
│   │   └── services/              # 業務邏輯
│   └── tests/test_core_logic.py
└── frontend/
    ├── package.json
    ├── next.config.ts             # CSP、inlineCss 等
    ├── .env.example               # 前端範本（可進版控）
    ├── .env.local                 # 本機（禁止 commit）
    └── src/
        ├── app/                   # 頁面與 BFF routes
        ├── components/
        └── lib/                   # api、brand、types、validate-url
```

**重要：** 前端 **沒有** `vercel.json`；Vercel 部署時 Root Directory 必須設為 `frontend`。

---

## 3. 本機開發與啟動

### 3.1 前置需求

- Windows 建議；Python 3.12+、Node.js 20+（建議 LTS）
- 合併高畫質／多數 ASR 轉檔需要 ffmpeg（或依賴 `imageio-ffmpeg` 打包二進位）

### 3.2 一鍵啟動

```bat
start.bat
```

行為摘要：

1. 檢查 Python／Node  
2. 建立 `backend/.venv`（若不存在）  
3. 若不存在 `backend/.env` → 從 `.env.example` 複製  
4. 若不存在 `frontend/node_modules` → `npm install`  
5. 若不存在 `frontend/.env.local` → 寫入 `NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000`  
6. **新視窗**啟動 uvicorn（`--reload`，`:8000`）  
7. 本視窗 `npm run dev`（`:3000`）

僅後端：`start-backend.bat`。

### 3.3 手動指令

```bat
REM 後端
cd backend
.venv\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload --host 127.0.0.1 --port 8000 --proxy-headers

REM 前端
cd frontend
npm install
npm run dev
```

### 3.4 本機健康檢查

- 後端：`http://127.0.0.1:8000/health`  
- 前端代理：`http://localhost:3000/api/health`  
- UI：`http://localhost:3000`

`redis: "memory"` 代表未設定 `REDIS_URL`，屬正常本機行為。

---

## 4. 後端架構（FastAPI）

### 4.1 啟動 lifespan（`app/main.py`）

順序概念：

1. 建立 `TMP_DIR`  
2. `init_task_store` / `init_ttl_cache`（有 `REDIS_URL` 則 Redis，否則記憶體）  
3. 下載 semaphore、info／download thread pools  
4. 恢復中斷任務標記（canary／recover）  
5. 背景 `cleanup_loop` + `warmup_canary`（yt-dlp 探針）  
6. 關閉時取消背景任務、shutdown executors  

Middleware 鏈：`SecurityHeaders` → `Maintenance`（`MAINTENANCE_MODE` 時 `/api/*` 503）→ Gzip → ProxyHeaders → CORS。

### 4.2 Routers

| 模組 | 路徑 | 職責 |
|------|------|------|
| `health` | `GET /health` | 健康、canary、egress、維護狀態（保活用） |
| `info` | `POST /api/info` | 解析影片資訊與格式 |
| `download` | `POST /api/download` | 建立下載／字幕／ASR 任務 |
| `tasks` | `GET /api/tasks/{id}` | 任務進度輪詢 |
| `files` | `GET\|HEAD /api/files/{id}` | 取檔（302 直連或 proxy 檔案） |
| `stats` | `GET /api/stats`、`POST /api/presence` | 線上／PV 統計 |

### 4.3 Services（維護者地圖）

| 檔案 | 職責 |
|------|------|
| `ytdlp_service.py` | URL 驗證、extract、格式／字幕收集、直連 URL、下載合併、輸出檢查 |
| `worker.py` | 背景任務編排：直連優先、代理 fallback、ASR 流程、進度寫回 |
| `asr_service.py` | ffmpeg／音訊準備、Gemini Files API、字幕寫檔 |
| `url_utils.py` | YT／FB／IG 正規化、playlist 偵測、ANSI strip |
| `task_store.py` | 任務狀態 Memory／Redis |
| `ttl_cache.py` | 通用 TTL（info cache、egress、PV 計數等） |
| `egress.py` | 月代理流量累計與 soft／hard 壓力 |
| `presence.py` | 線上心跳 + 今日／累計 PV |
| `concurrency.py` | 全域併發 + 每 IP 上限 |
| `executors.py` | info／download 分離線程池 |
| `rate_limit.py` | IP token bucket |
| `cleanup.py` | 過期任務與暫存檔清除 |
| `disk.py` | 磁碟空間下限 |
| `canary.py` | yt-dlp 探針；重啟後標記中斷任務 |
| `turnstile.py` | 可選 Cloudflare Turnstile（需前端配合） |
| `redis_meter.py` | Upstash 日命令數軟降級 |

### 4.4 直連 vs 代理（必讀）

| 路徑 | 行為 | 計入月流量？ |
|------|------|----------------|
| **直連（redirect）** | 解析出 googlevideo 等 URL，`delivery=redirect`，瀏覽器直接抓 | **否**（伺服器不傳 payload） |
| **代理（proxy）** | 伺服器 yt-dlp 下載到 `TMP_DIR`，再 `FileResponse` | **是** |

**悲觀假設：** 雲端 IP 解析出的直連 URL 常綁定伺服器 IP，使用者端 302 後可能 403，最終仍走代理。因此 Render Blueprint 採用較低月額度（約 40GB）做容量規劃；本機 `config.py` 預設約 90GB。**實際以部署環境變數為準。**

壓力等級（`bandwidthPressure` / `outbound_pressure`）：

| 等級 | 條件（約） | 行為 |
|------|------------|------|
| `ok` | 用量 &lt; soft | 正常列出格式 |
| `soft` | ≥ 70% | 隱藏高畫質代理選項（保留直連與較低代理） |
| `hard` | ≥ 92% | 禁止新的代理下載；僅直連／字幕等輕量路徑 |
| 用盡 | used ≥ limit | `/health.egressExhausted=true` → 前端大字彈窗 |

### 4.5 YouTube 抽取注意

- Mix／電台網址會正規化成純 `watch?v=VIDEO_ID`（去掉 `list=RD…` 等）。  
- 影片 ID 必須 **11 碼**。  
- `allowed_extractors` 使用可涵蓋 YouTube 且擋 generic 的設定（避免 `No suitable extractor`）。  
- 錯誤訊息須 `strip_ansi`，勿把 `[0;31m` 吐到前端。  
- 格式選擇器：progressive＝單一 itag（可直連）；adaptive＝`itag+audio` 等；**禁止**靜默加 `/b` 讓使用者拿到錯誤低畫質檔。  
- Player clients 多組嘗試；字幕欄位可跨 client 合併。

### 4.6 ASR 注意

- 僅當影片**沒有**既有字幕軌時 UI 才引導 ASR。  
- `geminiApiKey` **只在該次請求／記憶體**傳遞，**不寫入** Redis／task_store。  
- ASR **不做**跨請求 dedupe。  
- 需下載音訊 → 通常走代理路徑 → 會計 egress。  
- `MAX_ASR_DURATION_SECONDS=0` 表示不限制時長。

---

## 5. 前端架構（Next.js）

### 5.1 路由

| 路徑 | 說明 |
|------|------|
| `/` | 品牌首頁 + SitePresence + DownloaderLazy |
| `/privacy` | 隱私權政策 |
| `/disclaimer` | 免責聲明 |
| `/robots.txt`、`/sitemap.xml` | SEO |
| `/api/health` | 同源代理後端 `/health`（避 CORS／PageSpeed 雜訊） |
| `/api/presence`、`/api/stats` | 同源代理；後端失敗時進程內 fallback 計數 |

**已刪除（勿再加回）：**  
`frontend/src/app/api/{info,download,tasks,files}` 與 `mock-store.ts`。  
下載相關 API **一律**打 FastAPI（`NEXT_PUBLIC_API_BASE_URL`）。

### 5.2 關鍵元件

| 元件 | 用途 |
|------|------|
| `DownloaderLazy` | 輕量殼；解析通過後才 dynamic import `Downloader` |
| `Downloader` | 主流程：info、模式、下載、輪詢、egress 彈窗 |
| `FormatPicker` | 畫質；進階 MP4 需密碼 `0000`（前端硬編碼，產品設計） |
| `SubtitlePicker` | 字幕／ASR + Gemini key 輸入 |
| `ProgressPanel` | 進度與自動觸發下載 |
| `CookieConsent` | Cookie／本機儲存同意（`useSyncExternalStore`） |
| `SitePresence` | 同意後每 ~25s heartbeat |
| `TrafficExhaustedModal` | 月流量用盡 |
| `ThemeProvider`／`ThemeToggle` | 明暗主題 |
| `JsonLd`／`SiteFooter`／`SecurityPattern` | SEO／頁尾／背景 |

### 5.3 `lib/api.ts` 行為

- `NEXT_PUBLIC_API_BASE_URL` 有值：瀏覽器直接請求後端 `/api/info`、`/api/download`、`/api/tasks/*`。  
- Health／presence／stats：走**同源** `/api/*` BFF。  
- Info 前端記憶體 cache：約 5 分鐘，key 形如 `v3|url|container`。  
- 預設畫質挑選：優先 **360p progressive（可直連）**。

### 5.4 本機儲存 keys（勿混淆）

| Key | 用途 |
|-----|------|
| `dlyt-theme` | 主題 `light`／`dark` |
| `zenith.cookieConsent` | `accepted`／`essential` |
| `zenith.visitorId` | 匿名訪客（僅全部允許後） |
| `dlyt.geminiApiKey` | sessionStorage；使用者自填的 Gemini key（僅瀏覽器） |

### 5.5 品牌常數（`lib/brand.ts`）

- `BRAND_NAME`：巔峰思維  
- `BRAND_URL`：https://www.getzenithmind.com/zh-TW  
- `BRAND_EMAIL`：privacy@getzenithmind.com  

修改品牌時同步更新：`brand.ts`、layout metadata、privacy／disclaimer、JsonLd、README。

### 5.6 URL 驗證（`lib/validate-url.ts`）

- `isValidMediaUrl`／`canonicalizeMediaUrl`：YT＋FB＋IG。  
- 前端已不再使用僅 YouTube 的驗證函式；統一走 media URL。

---

## 6. 核心業務流程

### 6.1 解析影片

```
使用者貼網址 → canonicalize → POST /api/info
  → rate limit →（可選 Turnstile）
  → extract_video_info（多 client／cache）
  → VideoInfo（formats、advancedFormats、subtitles、bandwidthPressure）
```

### 6.2 下載影片

```
POST /api/download { mode: video, formatId, ... }
  → 額度／proxy 允許檢查 → taskId
  → worker：可直連？ → redirect 完成
         否則 allow_proxy_fallback？ → yt-dlp 下載 → proxy 檔案
  → 前端輪詢 GET /api/tasks/{id}
  → 完成後開啟 downloadUrl（同源後端或 PUBLIC_BASE_URL）
```

### 6.3 字幕／ASR

```
mode=subtitle → 下載指定語言字幕檔
mode=asr → 確認無既有字幕 → 下載音訊 → Gemini → 寫出 srt/vtt/txt
```

### 6.4 統計

```
Cookie「全部允許」→ SitePresence
  → POST /api/presence { visitorId, pageHit }
  → onlineNow / pageViewsToday / pageViewsTotal
```

---

## 7. API 契約完整清單

### 7.1 後端

| Method | Path | Body／參數 | 回應重點 |
|--------|------|------------|----------|
| GET | `/health` | — | 見下表 |
| POST | `/api/info` | `url`, `preferredContainer?`, `turnstileToken?` | `VideoInfo` |
| POST | `/api/download` | `url`, `mode`, `formatId?`, `subtitleLanguage?`, `subtitleFormat?`, `geminiApiKey?`, `asrLanguage?`, `turnstileToken?` | `{ taskId }` |
| GET | `/api/tasks/{taskId}` | — | `status`, `progress`, `message`, `downloadUrl`, `filename`, `error`, `delivery` |
| GET | `/api/files/{taskId}` | — | 302 或檔案串流 |
| HEAD | `/api/files/{taskId}` | — | 標頭／`X-Delivery` |
| GET | `/api/stats` | — | `onlineNow`, `pageViewsToday`, `pageViewsTotal` |
| POST | `/api/presence` | `visitorId`（≥8）, `pageHit?` | 同上 |

### 7.2 `/health` 欄位

| 欄位 | 意義 |
|------|------|
| `status` | `ok` 或 `maintenance` |
| `redis` | `memory`／`redis`／`redis-degraded` 等 |
| `ytdlp` | canary：`ok`／`degraded`／`skipped`／`unknown` |
| `ytdlpDetail` | 探針細節 |
| `outboundUsedBytes` | 本月已用代理位元組 |
| `outboundLimitBytes` | 月上限 |
| `egressExhausted` | 是否已用盡 |
| `maintenance` | 是否維護模式 |

### 7.3 任務狀態

`pending` → `running` → `completed`｜`failed`  
`delivery`：`redirect`｜`proxy`（或實作中的對應字串）。

---

## 8. 環境變數一覽

### 8.1 後端（`backend/.env.example`）

| 變數 | 建議正式環境 | 說明 |
|------|--------------|------|
| `REDIS_URL` | **必填建議** | Upstash `rediss://…`；空＝記憶體 |
| `CORS_ORIGINS` | **必填** | 前端網域，逗號分隔（含 Vercel URL） |
| `PUBLIC_BASE_URL` | **必填** | 後端對外 URL，例如 `https://xxx.onrender.com` |
| `TMP_DIR` | `/tmp/dlyt` | 暫存下載 |
| `TASK_TTL_SECONDS` | 3600 | 任務狀態存活 |
| `FILE_TTL_SECONDS` | 1800 | 檔案存活 |
| `FILE_GRACE_SECONDS` | 300 | 下載後寬限清除 |
| `MAX_FILESIZE_BYTES` | 0 | 0＝不限單檔 |
| `MAX_DURATION_SECONDS` | 0 或依方案 | 0＝不限；Render yaml 可能設 7200 |
| `MIN_FREE_DISK_BYTES` | 200MB | 磁碟下限 |
| `RATE_LIMIT_PER_MINUTE` | 30 | 每 IP |
| `MAX_CONCURRENT_DOWNLOADS` | 2 | 全域併發 |
| `MAX_ACTIVE_TASKS_PER_IP` | 2 | 每 IP |
| `DOWNLOAD_TIMEOUT_SECONDS` | 1200 | 大檔合併；**render.yaml 預設 300 偏短，可視需要覆寫** |
| `INFO_WORKERS` | 4 | info 線程池 |
| `INFO_CACHE_TTL_SECONDS` | 180–300 | extract 快取 |
| `FAIL_CACHE_TTL_SECONDS` | 45 | 失敗快取 |
| `CANARY_URL` | 穩定公開 YT | 啟動探針 |
| `SENTRY_DSN` | 可選 | 錯誤追蹤 |
| `LOG_JSON` | true | JSON log |
| `MAINTENANCE_MODE` | false | true 時 `/api/*` 503 |
| `PREFER_DIRECT_DOWNLOAD` | true | 優先直連 |
| `ALLOW_PROXY_FALLBACK` | true | 直連失敗是否代理 |
| `MAX_MONTHLY_OUTBOUND_BYTES` | 依預算 | 本機預設 ~90GB；render.yaml ~40GB |
| `OUTBOUND_SOFT_RATIO` | 0.70 | soft 閾值 |
| `OUTBOUND_HARD_RATIO` | 0.92 | hard 閾值 |
| `MAX_PROXY_HEIGHT` | 1080 | 代理最高畫質 |
| `REDIS_MAX_COMMANDS_PER_DAY` | 8000 | Upstash 軟降級 |
| `TURNSTILE_SECRET_KEY` | 可選 | 需前端 widget 才有用 |
| `TURNSTILE_REQUIRED` | false | **勿單獨改 true**（前端尚未接 widget） |
| `MAX_ASR_DURATION_SECONDS` | 0 | ASR 時長上限 |
| `ASR_TIMEOUT_SECONDS` | 1800 | ASR 逾時 |
| `GEMINI_ASR_MODEL` | gemini-2.5-flash | 模型名 |
| `GEMINI_INLINE_MAX_BYTES` | ~18MB | inline 上限 |

### 8.2 前端

| 變數 | 說明 |
|------|------|
| `NEXT_PUBLIC_API_BASE_URL` | **必填**。正式＝Render 後端 URL（無尾隨 `/`） |
| `NEXT_PUBLIC_TURNSTILE_SITE_KEY` | 可選；需實作 UI 後才有意義 |
| `NEXT_PUBLIC_SITE_URL` | 可選；sitemap／絕對 URL |

### 8.3 禁止上傳的檔案

| 檔案 | 原因 |
|------|------|
| `backend/.env` | 可能含 Redis／Sentry／Turnstile |
| `frontend/.env.local` | 本機 API 指向 |
| `backend/.venv/`、`frontend/node_modules/` | 依賴目錄 |
| `backend/tmp/` | 下載暫存 |
| 任何含 `AIza…`、真實 `REDIS_URL` 密碼、Turnstile secret 的檔案 | 金鑰 |

`.gitignore` 已涵蓋上述；`frontend/.gitignore` 對 `.env*` 有例外：`!.env.example`、`!.env.local.example`。

**伺服器不存放使用者 Gemini Key**；使用者 Key 只存在瀏覽器 sessionStorage 與單次 API 請求。

---

## 9. 部署（GitHub／Render／Vercel／Upstash）

### 9.1 建議順序

1. Push GitHub（不含密鑰）  
2. 建立 Upstash Redis → 取得 `REDIS_URL`  
3. Render 部署後端（Blueprint 讀 `render.yaml`）→ 取得後端 URL  
4. Vercel 部署前端（Root=`frontend`，設 `NEXT_PUBLIC_API_BASE_URL`）→ 取得前端 URL  
5. 回填 Render：`CORS_ORIGINS`、`PUBLIC_BASE_URL`、`REDIS_URL` → 重新部署  
6. GitHub Actions Secret：`BACKEND_HEALTH_URL`  
7. 煙霧測試：`/health`、首頁解析、下載、presence  

### 9.2 Render（後端）

- Blueprint：`render.yaml`  
- Service 名：`dlyt-api`；region：singapore；plan：free；health：`/health`  
- Docker：`backend/Dockerfile`（含 ffmpeg、Node 22、**wgcf + wireproxy**）  
- 啟動：`scripts/start.sh` 先起 Cloudflare WARP（userspace SOCKS `127.0.0.1:1080`），再起 uvicorn  
- **重要：** 容器必須監聽 Render 注入的 `PORT`  
- YouTube 機房 IP bot 檢查：正式環境預設 `ENABLE_WARP=true`，yt-dlp 走 `YTDLP_PROXY=socks5://127.0.0.1:1080`；`/health` 可看 `warp`／`ytdlpProxy`  
- 若 WARP 起不來會降級直連（YouTube 可能仍被擋）；下一步才是 cookies／付費住宅代理 

手動必填（`sync: false`）：

- `REDIS_URL`  
- `CORS_ORIGINS`（含 Vercel 網域與本機若需要）  
- `PUBLIC_BASE_URL`  
- （可選）`SENTRY_DSN`、`TURNSTILE_SECRET_KEY`  

### 9.3 Vercel（前端）

- Import GitHub repo  
- **Root Directory = `frontend`**  
- Environment Variable：`NEXT_PUBLIC_API_BASE_URL=https://<render-host>`  
- Deploy 後把實際網域寫進後端 `CORS_ORIGINS`

### 9.4 Keepalive

- Workflow：`.github/workflows/keepalive.yml`  
- Cron：每 12 分鐘  
- Repo Secret：`BACKEND_HEALTH_URL=https://<render-host>/health`  

免費方案仍可能偶發冷啟動（30–60 秒），屬正常。

### 9.5 自動升級 yt-dlp

- `.github/workflows/bump-yt-dlp.yml`：定期 bump 依賴並開 PR。  
- YouTube／FB／IG 常改協定，**應定期合併此類 PR**，否則解析會突然失敗。

### 9.6 部署後驗證清單

```text
[ ] GET https://<api>/health → status=ok，ytdlp 非長期 degraded
[ ] 瀏覽器開啟前端，同意 Cookie，數字有變化
[ ] 貼公開 YouTube 短片 → 解析成功 → 下載完成
[ ] 字幕模式可下載（若有字幕）
[ ] 後端 CORS 無瀏覽器擋下（Console 無 CORS error）
[ ] 未把 .env 推上 GitHub
```

---

## 10. 安全、合規與濫用防護

| 機制 | 狀態 | 維護建議 |
|------|------|----------|
| Rate limit | 已啟用（預設 30/分／IP） | 公開後可依濫用情況調低 |
| 併發上限 | 全域／每 IP | 免費方案勿調太高 |
| 月流量額度 | 已啟用 | 依帳單調整 `MAX_MONTHLY_OUTBOUND_*` |
| 維護模式 | `MAINTENANCE_MODE` | 緊急關閉下載 API |
| 免責／隱私 | `/disclaimer`、`/privacy` | 法務需求時更新文案 |
| Cookie 同意 | 已啟用 | 統計需「全部允許」 |
| Turnstile | 後端可選；**前端未接 UI** | 要防爬蟲需另開任務做 widget |
| CSP | `next.config.ts` | 改第三方腳本時同步調 CSP |
| 進階密碼 `0000` | 產品設計（非密鑰） | 若要改密碼，改 `FormatPicker.tsx` |

法律風險：下載工具有著作權爭議空間；維持免責聲明與必要時維護模式。

---

## 11. 測試與品質門檻

### 11.1 後端

```bat
cd backend
.venv\Scripts\python.exe -m pytest tests -v
```

涵蓋：URL／playlist、dedupe、cache key、container rank、字幕格式、無 ffmpeg WebM、rate limit、直連判定、ANSI、presence、egress。

### 11.2 前端

```bat
cd frontend
npm run lint
npm run build
```

輪詢相關 `exhaustive-deps` 已以註解刻意鎖定 `taskId`（避免每次 progress 重訂閱）。

### 11.3 推送前

參考根目錄 `測試報告.md`。重點：勿提交密鑰；確認 `.env.example` 可被追蹤。

---

## 12. 已知限制與技術債決策

| 項目 | 決策／現況 |
|------|------------|
| 直連成功率偏低 | **採納悲觀額度**；仍優先嘗試直連 |
| 4K／高畫質 | 代理最高 1080；進階 UI 可選更高但依來源／額度 |
| Mock API | **已刪除**；前端必須有真實後端 |
| Redis 本機為 memory | 正常；正式建議 Upstash |
| Turnstile | 後端就緒、前端未接 → 預設關閉 |
| FB／IG | 公開內容為主；登入牆／私人失敗屬預期 |
| Gemini Key | 使用者自備；伺服器無共用 Key |
| PageSpeed | 系統字體、lazy Downloader、同源 health、弱化重動畫 |

---

## 13. 故障排除手冊

| 症狀 | 可能原因 | 處理 |
|------|----------|------|
| `No suitable extractor` | extractor 白名單過嚴／yt-dlp 過舊 | 更新 yt-dlp；檢查 `allowed_extractors` |
| 只有極小檔／錯畫質 | 舊版靜默 fallback | 確認未再引入 `/b`；看實際 formatId |
| 直連 403 | IP 綁定 | 允許 proxy fallback；屬常見 |
| `redis=memory` 正式環境 | 未設 `REDIS_URL` | 設定 Upstash 並重佈 |
| CORS error | `CORS_ORIGINS` 未含前端網域 | 補網域重佈後端 |
| 解析慢／失敗 | YouTube 風控、client 失敗 | 看 canary；換 client；bump yt-dlp |
| YouTube bot 檢查 | 機房 IP | `/health` 應見 `warp=on`；Render logs 搜 `[warp]`；確認 `ENABLE_WARP=true` |
| WARP 起不來 | UDP／wireproxy 失敗 | logs 看 `wireproxy.log`；暫時改 cookies 或付費住宅代理 |
| ASR 失敗 | Key 無效、無 ffmpeg、逾時 | 檢查瀏覽器金鑰；Docker 應有 ffmpeg |
| 流量彈窗一直出現 | `egressExhausted` | 等月切或提高／重設計量（Redis key） |
| Render 第一次很慢 | Free 休眠 | 確認 keepalive secret |
| 前端打到 mock／404 | 舊程式或未設 API base | 確認 `NEXT_PUBLIC_API_BASE_URL`；勿恢復 mock |
| 無效影片 ID | 非 11 碼 | 前端／後端皆會擋 |

日誌：正式環境 `LOG_JSON=true`，可接 log drain／Sentry。

---

## 14. 維護 checklist 與變更紀錄

### 14.1 每月

- [ ] 檢查 Upstash／Render／Vercel 用量與帳單  
- [ ] 看 `/health` 的 `ytdlp`、`outboundUsedBytes`  
- [ ] 合併 `bump-yt-dlp` PR（若有）  
- [ ] 抽測 YouTube 解析＋下載  

### 14.2 每季

- [ ] 複審免責／隱私文案  
- [ ] 評估是否實作 Turnstile UI  
- [ ] 依賴安全：`npm audit`、Python 套件升級  

### 14.3 緊急

1. `MAINTENANCE_MODE=true` 重佈後端  
2. 公告／前端仍可開但 API 503  
3. 修復後改回 false  

### 14.4 變更紀錄

| 版本 | 日期 | 摘要 |
|------|------|------|
| v1.5 | 2026-08 | 直連悲觀額度、Gemini ASR、90GB 彈窗、FB／IG、進階 MP4 |
| v2.0 | 2026-08-05 | 品質修復（無靜默 fallback、ffmpeg、clients）；合規頁與 presence；刪 mock；完整維運文件；測試報告 |

---

## 附錄 A — 常見本機指令速查

```bat
REM 測試
backend\.venv\Scripts\python.exe -m pytest backend\tests -v
cd frontend && npm run lint && npm run build

REM 健康
curl http://127.0.0.1:8000/health
curl http://localhost:3000/api/health
```

## 附錄 B — 相關文件

- `README.md`：快速開始與免費上線步驟  
- `測試報告.md`：推送前驗證結果  
- `backend/.env.example`、`frontend/.env.example`：環境變數範本  
- `render.yaml`：Render 預設  

---

*維護原則：改下載／格式邏輯時優先保證「失敗要明確、成功畫質要對」；改部署時優先保證 CORS、PUBLIC_BASE_URL、REDIS_URL 三者一致；永遠不要把 API Key 寫進 repo。*
