# dlyt／巔峰思維 — 推送前完整測試報告

**日期：** 2026-08-05  
**環境：** Windows 10 · Python 3.12 · Next.js 16.3 · 本機 FastAPI `127.0.0.1:8000` + Next `localhost:3000`

---

## 總結

| 項目 | 結果 |
|------|------|
| 後端單元測試 | **14/14 PASS** |
| 前端 `npm run build` | **PASS** |
| 前端 `npm run lint` | **PASS**（0 error，2 warning） |
| 關鍵 API 煙霧測試 | **PASS** |
| 靜態頁面可達 | **PASS** |
| 冗餘／技術債清理 | **已完成** |

**結論：目前狀態適合推送到 GitHub。**（尚未代為 `git push`；需你明確要求才會推送。）

---

## 1. 後端單元測試（`pytest`）

指令：`backend/.venv/Scripts/python.exe -m pytest tests -v`

| 測試 | 結果 | 覆蓋功能 |
|------|------|----------|
| `test_youtube_url_valid` | PASS | YouTube URL 驗證 |
| `test_facebook_instagram_urls` | PASS | FB／IG URL 驗證與正規化 |
| `test_canonicalize_strips_mix_params` | PASS | Mix／playlist 參數剝除 |
| `test_playlist_detection` | PASS | 播放清單偵測 |
| `test_dedupe_key_stable` | PASS | 下載去重鍵 |
| `test_url_cache_key_stable` | PASS | info 快取鍵 |
| `test_container_rank_prefers_webm` | PASS | 容器偏好排序 |
| `test_segments_to_srt` | PASS | ASR → SRT／VTT／TXT |
| `test_ensure_audio_accepts_webm_without_ffmpeg` | PASS | 無 ffmpeg 時 WebM 直通 |
| `test_token_bucket_retry_after` | PASS | 速率限制 |
| `test_can_try_direct_delivery` | PASS | 直連交付條件 |
| `test_strip_ansi` | PASS | yt-dlp ANSI 清理 |
| `test_presence_heartbeat` | PASS | 線上人數／瀏覽計數（本次新增） |
| `test_egress_pressure_helpers` | PASS | 月流量壓力等級（本次新增） |

---

## 2. 後端 API 煙霧測試（真實服務）

| 功能 | 方法／路徑 | 結果 | 備註 |
|------|------------|------|------|
| 健康檢查 | `GET /health` | PASS | `status=ok`，`ytdlp=ok`，Redis=memory |
| 解析影片 | `POST /api/info`（Me at the zoo） | PASS | 回傳 id／title／formats／subtitles |
| 無效網址 | `POST /api/info`（example.com） | PASS | HTTP 400 |
| 建立下載 | `POST /api/download` mode=video | PASS | 取得 `taskId` |
| 任務進度 | `GET /api/tasks/{id}` | PASS | 快速 `completed`（直連 redirect） |
| 檔案端點 | `HEAD /api/files/{id}` | PASS | HTTP 200（redirect 交付） |
| 字幕下載 | `POST /api/download` mode=subtitle | PASS | 產出 `.en.srt` |
| 即時統計 | `POST /api/presence` | PASS | online／今日／累計 |
| 統計快照 | `GET /api/stats` | PASS | 與 presence 一致 |
| 前端代理健康 | `GET localhost:3000/api/health` | PASS | 同源代理後端 |

**未以真實公開貼文完整跑通的項目（僅單元驗證 URL 規則）：**

- Facebook／Instagram **真實**影片解析與下載（假 ID 回 400，屬預期；需真實公開連結再驗）
- Gemini ASR（需有效 API Key，本次未注入）
- 進階 MP4 密碼閘道（UI 流程，本次未做 E2E UI）
- Turnstile（預設未啟用）

---

## 3. 前端建置與靜態頁

| 檢查 | 結果 |
|------|------|
| TypeScript／Production build | PASS（清理後路由僅保留 health／presence／stats proxy） |
| ESLint | PASS：0 errors；Downloader／ProgressPanel 各 1× `exhaustive-deps` warning（輪詢 effect 刻意鎖定 `taskId`） |
| `/` `/privacy` `/disclaimer` `/robots.txt` `/sitemap.xml` | PASS（本機 Next 可達） |

---

## 4. 功能對照（產品能力）

| 功能模組 | 驗證方式 | 狀態 |
|----------|----------|------|
| YouTube 解析／下載／字幕 | 單元 + 即時 API | PASS |
| FB／IG URL 接受規則 | 單元測試 | PASS |
| FB／IG 真實下載 | — | 未測（需真實 URL） |
| 直連／redirect 交付 | 即時 API | PASS |
| Presence／Cookie 同意閘 | 程式碼審查 + API | PASS |
| 隱私／免責／Schema／Footer | 建置 + 頁面可達 | PASS |
| 月流量額度邏輯 | 單元 + health 欄位 | PASS |
| 主題切換 | 建置通過（hydration 改用 `useSyncExternalStore`） | PASS |
| 輕量殼 → Downloader lazy | 建置路由正常 | PASS |

---

## 5. 已刪除的冗餘／技術債

確認無用後直接刪除：

| 項目 | 原因 |
|------|------|
| `frontend/src/app/api/{info,download,tasks,files}` + `mock-store.ts` | 假資料 mock；`.env`／`start.bat` 一律走 FastAPI，且 mock 僅支援 YouTube、與現況不符 |
| `frontend/public/{next,vercel,globe,file,window}.svg` | create-next-app 預設圖示，專案未引用 |
| `frontend/CLAUDE.md`、`AGENTS.md`、`README.md` | 樣板文件；專案說明以根目錄 `README.md` 為準 |
| `isValidYouTubeUrl`（前端） | mock 刪除後無引用；統一用 `isValidMediaUrl` |

**順手修復（推送前品質）：**

- CookieConsent／ThemeProvider：改 `useSyncExternalStore`，消除 lint error
- SitePresence：同意撤回時不再同步 `setState(null)`
- Downloader：Gemini key 改 lazy `useState` 初始化
- 補 presence／egress 單元測試

---

## 6. 建議推送前注意

1. 確認未提交：`.env`、`.env.local`、`.venv`、`node_modules`、下載暫存。
2. 上線需設定：`NEXT_PUBLIC_API_BASE_URL`、`CORS_ORIGINS`、`PUBLIC_BASE_URL`；建議 `REDIS_URL`。
3. 若要補齊 FB／IG／ASR E2E，建議另開一輪以真實公開連結與測試 Key 驗證。

---

*本報告由推送前掃描與自動化／煙霧測試產生。*
